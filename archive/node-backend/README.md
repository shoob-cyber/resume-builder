This folder contains the original Node/Express backend that was archived after porting functionality to the FastAPI Python backend.

Files included:
- server.js
- routes/
- controllers/
- models/
- config/db.js

Keep this folder for reference. The active backend is now implemented in `backend/` (FastAPI + SQLAlchemy).
